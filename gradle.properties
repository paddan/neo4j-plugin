# Disable the IntelliJ Platform Gradle plugin's network call to JetBrains
# Marketplace for the latest plugin version. The check times out in
# network-isolated CI runners (e.g. Codeberg) and fails the build.
org.jetbrains.intellij.platform.selfUpdateCheck=false
